/*
 * celia.date.js v1.0.0
 * (c) 2018-2019 Jesse Feng
 * Released under the MIT License.
 */
import { M as MS_OF_DAY } from './_dateConsts-b029053d.js';
import repeat from 'celia/_repeat';
import isUndefined from 'celia/isUndefined';
import './isLeapYear.js';
import { d as daysInYear } from './_dayOfYear-bf03459b.js';

function dayOfYear (date, val) {
  var arr = daysInYear(date);
  var month = date.getMonth();
  var count = 0;
  repeat(0, month, function (i) {
    count += arr[i];
  });
  count += date.getDate();
  if (isUndefined(val)) {
    return count;
  } else {
    date.setTime(+date + ((val - count) * MS_OF_DAY));
    return date;
  }
}

export default dayOfYear;
