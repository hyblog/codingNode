/*
 * celia.date.js v1.0.0
 * (c) 2018-2019 Jesse Feng
 * Released under the MIT License.
 */
import 'celia/isNil';
import 'celia/_loop';
import { a as MS_OF_HOUR, b as MS_OF_MINUTE, c as MS_OF_SECOND } from './_dateConsts-b029053d.js';
import { Y as YEAR, M as MONTH, c as DATE, D as DAY, H as HOUR, a as MINUTE, S as SECOND, n as normalizeUnit, b as MILLISECOND } from './_normalizeUnit-4674c723.js';
import { s as startOfDate, a as startOfTime } from './_startOf-10b13b76.js';

function startOf (date, unit) {
  unit = normalizeUnit(unit, MILLISECOND);

  var time = 0;

  switch (unit) {
    case YEAR:
      time = startOfDate(date, null, 0, 1);
      break;
    case MONTH:
      time = startOfDate(date, null, null, 1);
      break;
    case DATE:
    case DAY:
      time = startOfDate(date);
      break;
    case HOUR:
      time = startOfTime(date, MS_OF_HOUR);
      break;
    case MINUTE:
      time = startOfTime(date, MS_OF_MINUTE);
      break;
    case SECOND:
      time = startOfTime(date, MS_OF_SECOND);
      break;
  }

  time && date.setTime(time);

  return date;
}

export default startOf;
