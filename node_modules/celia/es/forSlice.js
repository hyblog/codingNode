/*
 * celia.js v6.1.4
 * (c) 2018-2019 Jesse Feng
 * Released under the MIT License.
 */
import './_bindContext.js';
import forSlice$1 from './_forSlice.js';
import transIndex from './_transIndex.js';
import isFunction from './isFunction.js';

function forSlice (value, start, end, iterator, context) {
  if (value) {
    var len = value.length;
    if (isFunction(start)) { // forEach like
      context = end;
      iterator = start;
      end = len;
      start = 0;
    } else if (isFunction(end)) {
      context = iterator;
      iterator = end;
      end = len;
    }
    // 如果是负数，就需要转成长度对应的正数
    start = transIndex(start, len);
    end = transIndex(end, len);

    forSlice$1(value, start, end, iterator, context);
  }
}

export default forSlice;
