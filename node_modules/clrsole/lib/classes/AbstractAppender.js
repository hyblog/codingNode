'use strict';

const AbstractLevel = require('./AbstractLevel');
const NotImplementedError = require('../NotImplementedError');

class AbstractAppender extends AbstractLevel {

  /**
   * 构造函数
   * @param {String} name
   * @param {Object} options appender对应的配置
   */
  constructor(name, options) {
    super(options);
    this.name = name;
  }

  append() {
    throw new NotImplementedError('append');
  }

  destroy() {
    throw new NotImplementedError('destroy');
  }

}

module.exports = AbstractAppender;
